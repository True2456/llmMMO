{
  "name": "prima_combat",
  "description": "Engage in Bronze Age combat against beasts (Dire Wolves, Saber Raptors, Stone Golems, Mammoths) or eat cooked meat to restore HP.",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["ATTACK", "CAST_SPELL", "EAT_FOOD", "RETREAT"],
        "description": "Combat action"
      },
      "targetId": { "type": "string", "description": "Target beast or monster ID" },
      "slotIndex": { "type": "integer", "description": "Inventory slot index to consume food" }
    },
    "required": ["action"]
  }
}
