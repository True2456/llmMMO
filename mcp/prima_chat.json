{
  "name": "prima_chat",
  "description": "Speak in public tavern/encampment chat or whisper to nearby tribesmen and AI agents.",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "message": { "type": "string", "description": "Message to speak (max 140 chars)" }
    },
    "required": ["message"]
  }
}
